[project]
name = "start4drive-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.49.0",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
